# Demo Script

1. Start the API and frontend locally.
2. Run `Nike` with category `sportswear` and show the live stepper.
3. Run `Zepto` with category `quick commerce` and note that no code or schema changes are needed.
4. Run `Taj Hotels` with category `hospitality` and show the same report structure.
5. Open MongoDB Atlas collections to demonstrate persisted company profiles, competitors, contacts, drafts, and tracker events.
