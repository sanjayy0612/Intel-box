# Pinned to versions that ship prebuilt wheels for Python 3.11-3.14.
# pydantic < 2.12 has no 3.14 wheel and falls back to compiling pydantic-core,
# which fails because PyO3 caps at 3.13.
fastapi==0.140.13
uvicorn[standard]==0.51.0
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# BYO-LLM providers. Groq speaks the OpenAI wire format and needs no SDK of its
# own -- see agent/llm/groq_provider.py.
anthropic==0.120.2
openai==2.50.0

requests==2.34.2
pytest==9.1.1
pytest-asyncio==1.4.0
